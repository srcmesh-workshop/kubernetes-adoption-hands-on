apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
  labels:
    app: wp-svc
    environment: develop
spec:
  type: ClusterIP
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: wordpress