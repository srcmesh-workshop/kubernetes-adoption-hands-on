apiVersion: v1
kind: Service
metadata:
  labels:
    app: wordpress
  name: wordpress-svc
  namespace: user-27
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: wordpress
  sessionAffinity: None
  type: ClusterIP

