apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
  labels:
    app: wordpress
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    app: wordpress
  type: LoadBalancer